# Vite dev server proxies /api to the backend (see vite.config.ts) — no env
# vars needed for local dev. For a production build pointing at a deployed
# backend on a different origin, the app would need a VITE_API_BASE_URL
# build-time variable; not yet wired up since this build targets the dev
# proxy setup.
